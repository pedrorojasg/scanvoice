# OpenRouter API key used by api/extract.ts (server-side only, never bundled).
# Local dev: copy to .env.local (picked up by `vercel dev`) or export in your shell.
# Production: set in the Vercel project's environment variables.
OPENROUTER_API_KEY=
